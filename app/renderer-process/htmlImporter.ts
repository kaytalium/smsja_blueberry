export module htmlImporter{

    //get all the import of the main page from which script is called.
   const links = document.querySelectorAll('link[rel="import"]') 

   export function fileLoader( ){
       
   }
}